version: 1

deploy:
  steps:
    terraformInit:
      before:
        - pip install awscli --user
        - aws sts get-caller-identity
  onCompletion:
    - echo "$(date) sleeping" && sleep 5 && echo "$(date) slept"
    - echo "$(date) hook completed"
