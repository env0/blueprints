version:
  type: 1

deploy:
  steps:
    setupVariables:
      after:
        - echo "$PUBLIC_KEY_CONTENTS" > "~/.ssh/azure_test.pub"
        - echo "Created pub file"

destroy:
  onCompletion:
    - echo "done"  