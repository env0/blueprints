version:
  type: 1

deploy:
  steps:
    terraformPlan:
      before:
        - echo "$PUBLIC_KEY_CONTENTS" > "~/.ssh/azure_test.pub"
        - echo "Created pub file"

destroy:
  onCompletion:
    - echo "done"  