version: 1
deploy:
    steps:
        setupVariables:
          after:
            - aws --version
            - gcloud --version
            - az --version
            - dig +short myip.opendns.com @resolver1.opendns.com
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
