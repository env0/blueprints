version: 1

deploy:
  steps:
    terraformInit:
      before:
        - /opt/tfenv/bin/terraform version
        - aws --version
        - aws sts get-caller-identity
        - gcloud --version
        - az --version
