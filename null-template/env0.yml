version: 1
deploy:
    steps:
        setupVariables:
          after:
            - aws --version
            - gcloud --version
            - az --version
            - curl ifconfig.me
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
