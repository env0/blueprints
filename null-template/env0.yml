version: 1

deploy:
  steps:
    terraformInit:
      before:
        - (cd .. ; ls)
        - aws --version
        - gcloud --version
        - az --version


deploy:
  onCompletion:
    - (cd .. ; ls)
    
destroy:
  onCompletion:
    - (cd .. ; ls)
