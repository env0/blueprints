version: 1

deploy:
  steps:
    setupVariables:
      after: 
        - echo "Counting up to a gazilion"
        - >&2 bash -c "printf 'So Long %.0s' {1..999999}" && exit 1
        - echo "Finished counting !!!"
