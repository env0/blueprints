version: 1

deploy:
  steps:
    setupVariables:
      after: 
        - echo "Counting up to a gazilion"
        - bash -c "printf 'So Long %.0s' {1..999999} >&2 && exit 1" 
        - echo "Finished counting !!!"
