version: 1

deploy:
  onCompletion:
    - echo "completed"
  onSuccess:
    - echo "great success"
  onFailure:
    - echo "oh no..."

  steps:
    terraformPlan:
      before:
        - pip install --user jinja2
        - python simple.py

destroy:
  onCompletion:
    - echo "destroy completed"
  onSuccess:
    - echo "destroy success"
  onFailure:
    - echo "destroy fail"

  steps:
    terraformPlan:
      before:
        - pip install jinja2
        - python simple.py

